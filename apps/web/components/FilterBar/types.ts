export type Filters = {
  aiModel: string;
  sentiment: string;
  region: string;
  category: string;
  search: string;
};

export type FilterOptions = {
  aiModels: string[];
  sentiments: string[];
  regions: string[];
  categories: string[];
};
